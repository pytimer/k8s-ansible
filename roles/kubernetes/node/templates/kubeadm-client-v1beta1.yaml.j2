apiVersion: kubeadm.k8s.io/v1beta1
kind: JoinConfiguration
discovery:
  bootstrapToken:
    token: "{{ kubeadm_token }}"
    caCertHashes:
    - "sha256:{{ kubeadm_ca_hash.stdout }}"
    apiServerEndpoint: "{{ k8s_vip_dns }}:{{ kube_apiserver_secure_port }}"